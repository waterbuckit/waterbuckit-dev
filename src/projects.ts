type Project = {
  title: string;
  description: string;
  href: string;
};

const projects: Project[] = [
  {
    title: "pigeon-type",
    description: "A fun, speed-typing test. It's still a WIP.",
    href: "https://pigeon-type.waterbuckit.dev",
  },
];

export default projects;
